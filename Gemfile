source "https://rubygems.org"

gem "rspec", "~> 3.13"

group :test do
  # Buildkite Test Engine collector for Ruby (RSpec + minitest).
  # https://github.com/buildkite/test-collector-ruby
  gem "buildkite-test_collector"
end
